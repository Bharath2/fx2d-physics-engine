scene:
    size: [12, 8]  
    gravity: [0, -10]
    background: [255, 255, 255, 255]  #./assets/bricks.png

entities:
    ground:
        pose: [6, 0.5, 0]
        physics:
            elasticity:  0.6
            gravity_scale: 0.0              # scale gravity (float) (0.0 will disable effect of gravity)
            external_forces_enabled: false
            static_friction: 0.3           # used in calculation of contact forces
            dynamic_friction: 0.2          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [12, 1.0]      # width, height
            texture: [50, 50, 50, 255] 
            border_thickness: 5.0
        collision:
            geometry: 
                rectangle: [18, 1.0]      # width, height
    floor:
        pose: [6, 3.0, 0]
        physics:
            elasticity:  0.6
            gravity_scale: 0.0              # scale gravity (float) (0.0 will disable effect of gravity)
            external_forces_enabled: false
            static_friction: 0.3           # used in calculation of contact forces
            dynamic_friction: 0.2          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [6, 0.75]      # width, height
            texture: ./assets/wood.png  # color(RGBA) or image path
        collision:
            geometry: 
                rectangle: [6, 0.75]      # width, height
    ball:
        pose: [10, 6.0, 0]                     # x, y, theta ; orientation in degrees
        init_velocity:  [-6.0, 0, 0]           # won't be used for child links
        physics:
            mass: 5
            # inertia: 0.125                     # if not given will be calculated from visual geometry  
            gravity_scale: 1.0             # scale gravity (float) (0.0 will disable effect of gravity)
            velocity_damping: 0.0          # damping will be applied when the entity moves  
            elasticity:  0.8               # 0 to 1 used in calculating coefficient of restitution
            static_friction: 0.3           # used in calculation of contact forces
            dynamic_friction: 0.2          # used in calculation of contact forces
            external_forces_enabled: true  # if false external influences or collisions do not effect, except gravity
        visual:
            pose: [0, 0, 0]             # relative to body frame
            geometry: 
                circle: 0.5             # circle, rectangle, polygon
            texture: ./assets/ball.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255] 
            border_thickness: 5.0
        collision:
            pose: [0, 0, 0]          # relative to body frame
            geometry: 
              circle: 0.5           # circle, rectangle, polygon
    box1:
        pose: [5.25, 3.75, 0]                     # x, y, theta
        physics:
            mass: 1
            elasticity: 1.0
            static_friction: 0.5           # used in calculation of contact forces
            dynamic_friction: 0.3          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
            texture: ./assets/rock.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255]
            border_thickness: 2.5
        collision:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
    box2:
        pose: [6.25, 3.75, 0]                     # x, y, theta
        physics:
            mass: 1
            elasticity: 1.0
            static_friction: 0.5           # used in calculation of contact forces
            dynamic_friction: 0.3          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
            texture: ./assets/rock.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255]
            border_thickness: 2.5
        collision:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
    box3:
        pose: [7.25, 3.75, 0]                     # x, y, theta
        physics:
            mass: 1
            elasticity: 1.0
            static_friction: 0.5           # used in calculation of contact forces
            dynamic_friction: 0.3          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
            texture: ./assets/rock.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255]
            border_thickness: 2.5
        collision:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
    box4:
        pose: [5.75, 4.5, 0]                     # x, y, theta
        physics:
            mass: 1
            elasticity: 1.0
            static_friction: 0.5           # used in calculation of contact forces
            dynamic_friction: 0.3          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
            texture: ./assets/rock.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255]
            border_thickness: 2.5
        collision:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
    box5:
        pose: [6.75, 4.5, 0]                     # x, y, theta
        physics:
            mass: 1
            elasticity: 1.0
            static_friction: 0.5           # used in calculation of contact forces
            dynamic_friction: 0.3          # used in calculation of contact forces
        visual:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
            texture: ./assets/rock.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255]
            border_thickness: 2.5
        collision:
            geometry: 
                rectangle: [0.75, 0.75]      # width, height
    box6:
        pose: [6.25, 5.25, 0]                     # x, y, theta
        physics:
            mass: 1
            elasticity: 1.0
            static_friction: 0.5           # used in calculation of contact forces
            dynamic_friction: 0.3          # used in calculation of contact forces
        visual:
            geometry:
                rectangle: [0.75, 0.75]      # width, height
            texture: ./assets/rock.png  # color(RGBA) or image path
            border_color: [0, 0, 0, 255]
            border_thickness: 2.5
        collision:
            geometry:
                rectangle: [0.75, 0.75]      # width, height
    capsule_demo:
        pose: [2.5, 6.0, 0.5]                    # x, y, theta (radians)
        physics:
            mass: 1
            elasticity: 0.5
            static_friction: 0.4
            dynamic_friction: 0.25
        visual:
            geometry:
                capsule: [1.2, 0.25]              # [segment_length, end_cap_radius]
            texture: [80, 160, 220, 255]
            border_color: [0, 0, 0, 255]
            border_thickness: 2.0
        collision:
            geometry:
                capsule: [1.2, 0.25]
    rounded_box_demo:
        pose: [9.5, 6.5, 0]
        physics:
            mass: 1
            elasticity: 0.6
            static_friction: 0.4
            dynamic_friction: 0.25
        visual:
            geometry:
                rectangle: [0.9, 0.9]
                radius: 0.15                      # Minkowski-sum skin -> rounded corners
            texture: [220, 140, 80, 255]
            border_color: [0, 0, 0, 255]
            border_thickness: 2.0
        collision:
            geometry:
                rectangle: [0.9, 0.9]
                radius: 0.15