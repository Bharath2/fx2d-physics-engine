# Intentional / third-party noise. Keep this file small — prefer fixing real issues.
missingIncludeSystem
unusedFunction
# Informational branch-limit notice; must not fail --error-exitcode=1.
normalCheckLevelMaxBranches
# Test harness uses throw for require() failures from main().
throwInEntryPoint:tests/*
# Interactive demo callbacks are invoked by the renderer's application loop.
throwInEntryPoint:examples/bucket_fill/main.cpp
throwInEntryPoint:examples/chain_terrain/main.cpp
# Registry stores non-const entity pointers by design (broad-phase mutates).
constVariablePointer:include/Fx2D/Registry.h
# Intentional override that also clears side caches (AABB tree, pair filters).
duplInheritedMember:include/Fx2D/Registry.h
